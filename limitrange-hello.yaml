apiVersion: v1
kind: LimitRange
metadata:
  name: default-resource-limits
  namespace: azure-arc  # or any vCluster namespace
spec:
  limits:
  - default:
      cpu: 200m
      memory: 256Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container
